# דף ההדרכה החיה — חצי מיליון לכל ילד · 7.9.26

הבנייה מחדש של `lp.smoove.io/ifej` אחרי הפידבק של גארי (25/08) ואחרי תיקון הבאגים שהיו בדף.
נבנה בשפת העיצוב של שאר הדפים (`~/mama-club/DESIGN.md`), לא בבנאי של סמוב.

## מצב
🟢 **חי מ-27/08/26:** https://adinahirr-commits.github.io/webinar-500k/
הכותרת אושרה, הפורטל נפרס, ההרשמה נבדקה מקצה לקצה מהדומיין החי.

## קבצים
| קובץ | מה זה |
|---|---|
| `index.html` | הדף עצמו |
| `thanks/index.html` | דף התודה. יורה את אירוע ה-Lead בפיקסל, ומציע הוספה ליומן |
| `COPY.md` | כל הקופי כטקסט נקי, אם תעדיפי להדביק לסמוב במקום לפרסם את הדף הזה |
| `assets/` | פורטרטים, לוגו, ארבע המלצות |

## איך זה מחובר
```
הטופס בדף
   └─> POST academy.mominvest.co.il/webinar-signup   (w=w0709)
          └─> רשימת סמוב 1163758 "וובינר חצי מליון לכל ילד 7/9/26"
   └─> thanks/  ->  Meta Pixel: Lead {content_name:'webinar-0709'}
```
- **רשימת סמוב 1163758 נוצרה ונבדקה.** הכנסתי איש קשר סינתטי, ראיתי שהוא נכנס, והסרתי אותו. הרשימה על 0.
- הפיקסל `1820166155346211`, אותו אחד של שאר הדפים. PageView בדף, Lead בדף התודה בלבד (כדי שלא ייספר פעמיים).
- פרמטרי `ref` ו-`utm_*` נשמרים ב-localStorage ועוברים הלאה, בדיוק כמו ב-guide1.

## ניתוב ההרשמות בפורטל ✅ חי
ב-`~/course-portal/app.py` יש ניתוב לפי הפרמטר `w`, כדי שההרשמות ייכנסו לרשימה של 7.9 ולא לרשימת וובינר החשיפה. **נפרס ל-Render ואומת בפרודקשן** (ליד סינתטי מהדומיין החי נכנס ל-1163758, ונמחק).

השינוי קטן ותואם לאחור (דף שלא שולח `w` ממשיך להתנהג בדיוק כמו קודם):
```python
WEBINARS = {"w0709": os.environ.get("WEBINAR_0709_LIST_ID", "1163758")}

def _webinar_list(data):
    return WEBINARS.get((data.get("w") or "").strip().lower(), WEBINAR_LIST_ID)
```

## מה עוד פתוח
1. ~~הכותרת~~ — ✅ אושרה 27/08/26, נעולה ב-`COPY.md`.
2. **המלצה עם מספר** — גארי צדק, ארבע ההמלצות בדף הן המלצות חוויה ("היה מרתק", "פתחת את המוח"). אף אחת מהן לא מוכיחה את ההבטחה של חצי מיליון. לכן הזזתי אותן למטה, לסקשן "מה אמרו אחרי ההדרכות הקודמות", ובראש הדף שמתי שורת מספרים אמיתיים במקומן. **אם תמצאי בוואטסאפ הודעה של בוגרת שאומרת מספר או פעולה שהיא ביצעה, זה שווה יותר מארבע ההמלצות האלה ביחד.**
3. **דומיין** — כרגע הדף מיועד ל-GitHub Pages תחת `adinahirr-commits.github.io`, שכבר מורשה ב-CORS של הפורטל. אם תרצי דומיין משלו צריך CNAME ב-box.co.il, ולהוסיף את הדומיין ל-`WEBINAR_ALLOWED_ORIGINS`.
4. **הזום** — הלינק נשלח מסמוב. צריך לבנות אוטומציה על רשימה 1163758 (מייל אישור מיידי + תזכורת יום לפני + תזכורת 30 דקות לפני).

## תצוגה מקדימה מקומית
```bash
cd ~/webinar-500k-0709 && python3 -m http.server 4810
# http://localhost:4810
```

## מה נבדק
- ✅ אין גלישה אופקית ב-500 / 768 / 1280 פיקסלים
- ✅ הרשמה לרשימת סמוב 1163758 עובדת מקצה לקצה (עם איש קשר סינתטי, שנמחק)
- ✅ הדף קריא במלואו גם כשה-JavaScript כבוי
- ✅ ספירה לאחור מכוונת ל-7.9.26 20:00 שעון ישראל
- ✅ ניווט מקלדת, פוקוס נראה, alt לכל התמונות, שלושת המסמכים המשפטיים
